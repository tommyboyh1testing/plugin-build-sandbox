# hello-world

A no-op example skill so the `skills/` directory has baseline content on main.

This plugin has no `build:` section, so the Phase 2 Build Verification workflow
will mark it `SKIPPED` for this baseline plugin. Fork PRs that add a plugin with
a `build:` section will trigger the full build chain.
